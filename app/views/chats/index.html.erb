<%= render "dashboard/menu" %>

<section class="tw-card" data-controller="chat">
  <h1 class="mb-2 text-2xl font-bold tracking-tight text-slate-900"><%= t('diet.suggestions') %></h1>
  <p class="mb-4 text-sm text-slate-600"><%= t('chat.describe_goals') %></p>

  <% if @total_pages > 1 %>
    <div class="mb-3 flex items-center justify-between">
      <span class="text-xs text-slate-500">Page <%= @page %> of <%= @total_pages %></span>
      <div class="flex gap-2">
        <% if @page > 1 %>
          <%= link_to chats_path(page: @page - 1), class: "inline-flex items-center gap-1 rounded-lg border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-slate-700 hover:bg-slate-50 no-underline" do %>
            <i class="fa fa-chevron-left"></i> Older
          <% end %>
        <% end %>
        <% if @page < @total_pages %>
          <%= link_to chats_path(page: @page + 1), class: "inline-flex items-center gap-1 rounded-lg border border-slate-200 bg-white px-3 py-1 text-xs font-semibold text-slate-700 hover:bg-slate-50 no-underline" do %>
            Newer <i class="fa fa-chevron-right"></i>
          <% end %>
        <% end %>
      </div>
    </div>
  <% end %>

  <section class="max-h-[55vh] min-h-[300px] overflow-y-auto rounded-xl border border-slate-200 bg-slate-50 p-4" data-chat-target="conversation">
    <% if @chat_messages.empty? %>
      <p class="text-xs text-slate-400 italic">No messages yet.</p>
    <% end %>
    <% @chat_messages.each do |message| %>
      <% label = message.role == "assistant" ? "Diet suggester" : "You" %>
      <strong class="mt-3 inline-block text-[11px] font-semibold uppercase tracking-wide text-slate-500"><%= label %>:</strong>
      <pre class="mt-1 whitespace-pre-wrap rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm text-slate-800"><%= message.content %></pre>
    <% end %>
  </section>

  <%= form_with data: { action: "chat#generateResponse" }, html: { class: "mt-4 grid gap-3" } do |form| %>
    <div>
      <%= form.label :objective, t('chat.objective_label'), class: "tw-label" %>
      <%= form.select :objective,
            [
              [t('chat.objectives.weight_loss'), 'weight_loss'],
              [t('chat.objectives.hypertrophy'), 'hypertrophy'],
              [t('chat.objectives.running_performance'), 'running_performance'],
              [t('chat.objectives.general_health'), 'general_health']
            ],
            {},
            { class: "tw-input", data: { chat_target: "objective" } } %>
    </div>
    <div class="grid gap-3 md:grid-cols-[1fr_auto] md:items-end">
      <div>
        <%= form.label :message, t('chat.message'), class: "tw-label" %>
        <%= form.text_area :message, id: "prompt", rows: 3, class: "tw-input", placeholder: t('chat.example'), data: { chat_target: "prompt" } %>
      </div>
      <div>
        <%= form.submit t('send'), class: "tw-btn-primary w-full md:w-auto" %>
      </div>
    </div>
  <% end %>
</section>
