<%= render "dashboard/menu" %>

<turbo-frame id="main">
<h1 class="mb-4 text-2xl font-bold tracking-tight fit-text-primary"><%= t('diet.edit') %></h1>

<%= render 'form', diet: @diet %>
</turbo-frame>
